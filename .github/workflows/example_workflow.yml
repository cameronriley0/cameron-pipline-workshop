name: Example Workflow

# When should this run?

on:
  pull_request: 
    branch:
      - main

# What should this do

jobs:
  Example Workflow:
    runs-on: ubuntu-latest

    steps:
    - name: Say hello
      runs: echo "Hello world"